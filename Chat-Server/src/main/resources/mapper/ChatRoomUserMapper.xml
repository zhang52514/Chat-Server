<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.anoxia.chat.mapper.ChatRoomMemberMapper">

    <resultMap id="ChatRoomUserResultMap" type="cn.anoxia.chat.common.domain.ChatRoomMember">
        <id column="id" property="id" />
        <result column="room_id" property="roomId" />
        <result column="user_id" property="userId" />
        <result column="role" property="role" />
        <result column="joined_at" property="joinedAt" />
    </resultMap>

    <insert id="insert" parameterType="cn.anoxia.chat.common.domain.ChatRoomMember" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_room_member (room_id, user_id, join_time, create_time, update_time)
        VALUES (#{roomId}, #{userId}, #{joinTime}, NOW(), NOW())
    </insert>
    <!-- 查询私聊房间ID -->
    <select id="getPrivateRoomId" resultType="long">
        SELECT cru1.room_id
        FROM chat_room_member cru1
        JOIN chat_room_member cru2
        ON cru1.room_id = cru2.room_id
        AND cru1.user_id != cru2.user_id
        JOIN chat_room rm
        ON rm.room_id = cru1.room_id
        WHERE cru1.user_id = #{userId1}
        AND cru2.user_id = #{userId2}
        AND rm.assessment_id = #{assessment}
        LIMIT 1;
    </select>
    <!-- 查询用户加入的聊天室 -->
    <select id="selectUserRoom" resultMap="ChatRoomUserResultMap">
        SELECT * FROM chat_room_member WHERE user_id = #{uid}
    </select>

    <select id="selectAllMembersInRoomIds" resultMap="ChatRoomUserResultMap">
        SELECT * FROM chat_room_member
        WHERE room_id IN
        <foreach collection="roomIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
